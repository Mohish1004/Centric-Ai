# Server Configuration
server.port=8080

# Spring Datasource Configuration (H2 File-based Persistence for out-of-the-box local operation)
spring.datasource.url=jdbc:h2:file:./data/finance_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console configuration
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Production Database Configuration Examples (MySQL / PostgreSQL) ---
# Uncomment and configure below properties when deploying to Render / Railway
# spring.datasource.url=jdbc:mysql://localhost:3306/finance_db?useSSL=false&serverTimezone=UTC
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=secret
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Custom JWT Application Properties
app.jwtSecret=9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08
app.jwtExpirationMs=86400000

# AI Python Service Integration URL
app.aiServiceUrl=http://localhost:8000
